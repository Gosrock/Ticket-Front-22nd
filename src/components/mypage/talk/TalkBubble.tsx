type TalkBubbleProps = {
  nickName: string;
  createdAt: string;
  content: string;
  iComment: boolean;
};

const TalkBubble = ({
  nickName,
  createdAt,
  content,
  iComment,
}: TalkBubbleProps) => {
  return <></>;
};

export default TalkBubble;
